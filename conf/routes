# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

POST        /retrieve        controllers.RetrieveController.retrieve
GET         /status          controllers.StatusController.status